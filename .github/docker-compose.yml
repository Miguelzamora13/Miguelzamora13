version: "3"
services:
  app:
    build: .
    ports:
      - "4000:4000"
    volumes:
      - ".:/app"
name: Docker Image CI

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:

  build:

    runs-on: ubuntu-latest


